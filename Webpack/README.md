# Webpack

This folder contains the Webpack exercises for the ALU curriculum.

It includes:

- `task_0` basic setup with jQuery
- `task_1` production webpack config with lodash debounce
- `task_2` CSS and image handling
- `task_3` modules, dev server, and tree shaking
